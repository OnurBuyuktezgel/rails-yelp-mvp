<div class="container mt-5 p-5">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, as: :radio_buttons, collection: Review::RATINGS, wrapper: :vertical_collection_inline %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>
</div>
